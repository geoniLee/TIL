# TIL 4.17
<h3>알고리즘 문제 풀이</h3>
<h4>영어 끝말잇기</h4>

* 알고리즘
    * 시뮬레이션
    * map

* 아이디어
    1. map을 사용해 중복된 단어 여부 확인
    2. n으로 나눈 값과 나눈 나머지에 +1을 하여 답으로 전달

<h3>알고리즘 문제 풀이</h3>
<h4>올바른 괄호</h4>

* 알고리즘
    * 선형 탐색
    * 구현/시뮬레이션

* 아이디어
    1. 주어진 문자열을 한차례 순회하며 '('인 경우 stack에 넣음
    2. ')'인 경우 stack이 비어있는 경우 false 반환
    3. ')'인 경우 stack이 비어있지 않다면, stack을 pop()
    4. 끝까지 순회했을 때 stack이 비어있다면 true, 아니라면 false 반환

---
<h3>Ch3 HW</h3>
<h4>HW07 Pawn 클래스 3D 캐릭터 만들기</h4>

* Player(Drone)
    1. Collider, SkeletonMesh, SpringArm, Camera 추가
* PlayerController
    1. SubSystem을 가져와 IMC와 연결
* IA_Move 
    1. Trigger에서 FVector2D을 받고, Completed에서 0으로 초기화 하여 이동 구현
    2. 받은 FVector2D을 저장하여 Tick에서 DeltaTime을 곱하여 AddActorLocalOffset로 이동 구현
* IA_Rotation
    1. 마우스 입력값을 FVector2D로 받아 Actor는 Yaw값만 반영해서 회전하도록 구현
    2. SpringArm은 Actor와 동일하게 Yaw값을 반영 받고, Pitch는 -80~80까지의 값만 제한해서 적용 받도록 구현