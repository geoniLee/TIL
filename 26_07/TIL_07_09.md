# TIL 7.09
<h3>코딩 테스트</h3>
<h4>Rearrange Words in a Sentence</h4>

* 알고리즘
    * 문자열 처리
    * 정렬

* 아이디어
    1. 받은 string을 stringsteam으로 변환
    2. `>>` 을 통해 공백을 기점으로 string을 분리하고 tolower을 통해 소문자로 변환하여 vector에 저장
    3. stable_sort을 사용해 길이를 비교하여 정렬
    4. 첫번째 string의 첫번째 문자만 대문자로 바꿔 정렬된 순으로 string에 넣어 반환

---
<h4>Ch04 팀 프로젝트</h4> 

1. Spanner 넉백 처리 개선
    * 기존 문제
        
        Host가 Spanner를 사용했을 때와 Host가 아닌 Client가 Spanner를 사용했을 때, 같은 값으로 넉백을 줘도 피격 대상이 밀리는 정도가 다르게 보였음
    * 변경 내용
        
        피격 캐릭터에 대한 넉백 적용을 AddImpulse 방식에서 LaunchCharacter 방식으로 변경

2. 아이템 슬롯 UI 갱신 Broadcast 조건 정리

    * 기존 문제
        슬롯 데이터 변경은 서버에서 처리되고 복제되는 것이 맞지만, Dedicate Server을 가정하고 구현하여 Listen Server의 Host을 고려할 필요가 생김

    * 변경 내용 
        
        OnItemSlotChanged.Broadcast()가 조종중인 Client에만 보내도록 수정
