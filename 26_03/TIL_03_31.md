# TIL 3.31
<h3>알고리즘 문제 풀이</h3>
<h4>1967 트리의 지름</h4>

* 알고리즘
    * dfs
    * 트리
* 아이디어
    1. 노드 1에 대한 dfs을 돌려 각 갈래의 끝 점을 저장
    2. 각 갈래의 끝 점에서 dfs을 돌려 최대 

---
<h4>HW 02 팀프로젝트</h4>

* 구현 사항
    - GameFlowManager
        - 추가된 3스테이지 연결
        - BattleManager에서 GameClear 여부 갱신 안되는 사항 수정
        - cout 출력 LogSystem으로 변경(색상 Cyan)
        - Item이 None으로 들어오는 오류 수정(ItemManager 초기화 위치)
        - 아이템 구매, 판매 시 인벤토리 반영되도록 갱신
        - 로비에서 인벤토리 조작 추가(장비 장착, 아이템 사용, 인벤토리 닫기)
            - 장비 장착, 아이템 사용 시 인벤토리 갱신하여 반영
    - GameManager
        - 레벨업 이후 Job 접근 불가 오류 수정
        - Boss 생성 위치 이전(BattleManager / BattleSupply)
    - Inventory→useItem() 입력된 아이템이 포션이 아닐 경우 Log 추가
* 트러블 슈팅
    * 전투 후 아이템 드랍 시 None으로 드랍 -> ItemManager에서 초기 설정을 해야 정상적으로 드랍 되는데 해당 시점 상점에 첫 방문 시점이 ItemManager 초기화 시점 -> 플레이어 생성 이후로 초기화 시점 변경
    * 선택지의 일부가 보이지 않는 현상 발생 -> LogSystem의 로그와 cout 혼용 사용 결과 Log가 길게 출력될 결우 cout에 가려지는 현상 발생 -> Log에 아래 공백 string을 추가
    * 골드 사용시 골드 갱신되지 않음 -> int& getGold로 gold을 받아와 갱신하여 set함수를 사용할 때와 다르게 status가 갱신되지 않음 -> 아이템 구매/판매가 끝날 때마다 상태창 갱신
    * 인벤토리 장비 장착, 아이템 사용 시 바로 반영되지 않음 -> 인벤토리의 선택지 출력전 인벤토리를 갱신하여 수정