# TIL 3.30
<h3>알고리즘 문제 풀이</h3>
<h4>23749 카드 컨트롤</h4>

* 알고리즘
    * bfs
    * map
* 아이디어
    1. vector의 push_back을 활용하기 위해 입력 값을 reverse
    2. queue을 사용해서 vector<char>와 이동 횟수를 저장
    3. map으로 키 - vector<char>, 방문 여부로 저장
    4. queue front의 vector에서 이긴 횟수가 더 높을 경우 이동 횟수 출력 후 종료 
    5. 각 순서에 있는 원소를 하나씩 뒤로 보내면서 방문 여부 확인 후 queue에 추가
---
<h4>HW 02 팀프로젝트</h4>

* 구현 사항
    * GameManager
        * GameManager에서 인벤토리 접근 추가
        * 캐릭터 직업 추가로 인한 GetInstance에 Job 추가
        * 현재 레벨(스테이지)에 따라 생성되는 몬스터 선정
        * 10레벨일 경우 보스 출현
    * GameFlowManager
        * 캐릭터 직업 추가로인한 GameFlowManager에 직업 선택 과정 추가
        * 레벨에 따른 스테이지 이동(1~3 / 4~6 / 7~9 / 10)
        * 전투 종료 시 플레이어의 경험치(100이상)에 따라 레벨업
        * LogSystem에 이름, 직업 이름 전달
        * 선택지에 플레이어 스텟 열람 추가(UI 구현 관련해서 빠질 가능성 있음)
        * 상점 이동 구현
            * 아이템 구매 연결
            * 아이템 판매 연결
        * 상점 이동 시 체력 회복
        * 게임 클리어를 판정할 수 있도록 관련 변수 Set 함수 추가
        
* 트러블 슈팅
    * GetCurrentStage와 Stage의 GetType() 반환 값 변경으로 인한 오류 GameFlowManager Type 변경으로 해결
    * BattleManager 생성 방식 변경으로 인한 GameFlowManager에서의 생성 방식 변경
    * 상점 아이템 세팅되지 않음 -> ItemManager init Setting 안해서 발생 -> 상점 초기화 전 ItemManager 초기화 진행
    * include<windows.h>와 함수명 혼용 문제로 함수명 변경(GetJop -> GetCurrentJob)