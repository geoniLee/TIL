# TIL 5.14
<h3>알고리즘 문제 풀이</h3>
<h4>이중 우선순위큐</h4>

* 알고리즘
    * multiset
    * 균형 이진 탐색 트리

* 아이디어
    1. operator을 D/I의 명령과 숫자로 구분해서 저장
    2. I일 경우 숫자를 multiset에 저장
    3. D일 경우 양수면 최댓값 제거(prev(ms.end()))
    4. D일 경우 음수면 최솟값 제거(ms.begin())
    5. 최댓값, 최솟값을 반환

---
<h3>Ch03 TeamProject</h3>
<h4>웨이브 정보 UI 연결</h4>

* Wave 갱신 
    * Wave 시작 시 델리게이트 보내서 Text 갱신하고 애니메이션 실행

* Enemy 수 갱신
    * Enemy Spawn, 제거시 Delegate을 보내 살아있는 Enemy 수 UI 에 반영

<h4>Enemy 정보 UI 연결</h4>

* Player의 CameraTrace가 Enemy을 감지하면 UI에 표시
    * Enemy의 EnemyType을 이름 위치에 갱신
    * Enemy의 현재, 최대 체력을 가져와 UI에 갱신
    * Enemy 체력 변동, 제거 시점에 Delegate 받아 갱신

<h4>BossEnemy 구현 방향 설정</h4>

* 구조
    ```
    ATGBossBase : AActor
    UTGBossPhaseBase : UObject
    UTGBossPhase1 : UTGBossPhaseBase
    UTGBossPhase2 : UTGBossPhaseBase
    UTGBossPhase3 : UTGBossPhaseBase
    ```

* 상태 패턴 사용
* 각 BossPhase가 패턴을 가지고 있고 조건을 판별하여 패턴 실행까지 담당
* 이동 X
* 생성시 생성 위치의 Tower 제거 고려중
* 부위 파괴 기능 구현 예정