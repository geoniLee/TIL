# TIL 6.22
<h3>알고리즘 문제 풀이</h3>
<h4>프렌즈 4블록</h4>

* 알고리즘
    * 시뮬레이션
    * 완전 탐색
    * Set

* 아이디어
    1. erase을 쉽게 사용하기 위해 0,0을 마지막 string의 0번째, 높이을 이동하면 string의 위치를 1씩 이동, 길이를 이동하면 이전 string의 해당 높이로 이동하는 vector을 생성
    2. set을 오름차순으로 정렬하도록 하여 생성
    3. vector을 순회하며, 우측, 우측 하단, 하단이 일치하는 위치를 찾고 4가지 값이 모두 일치한다면, 4개의 위치를 set에 추가
    3. set의 크기를 반환할 값으로 초기화
    4. set의 값을 하나씩 꺼내 erase
    5. 순회하며 같은 값을 찾은 결과가 0이 아닌 경우 반환 값을 answer에 합산, 0인 경우 지금까지 합산된 결과를 반환

---
<h3>멀티 </h3>
<h5>언리얼 모듈과 플러그인</h4>

* Module (Test)생성
    
    * 프로젝트 이름.Target.cs에 ExtraModulesNames에 추가
    * 프로젝트 이름Editor.Target.cs에 ExtraModulesNames에 추가
    * 프로젝트 이름.Build.cs에 PrivateDependencyModuleNames에 추가
    * 프로젝트 Character가 로그를 출력하도록 함

* PlugIn (Temporary)생성

    * PlugIn이름.build.cs 생성
    * 프로젝트 이름.uproject의 Plugins에 만든 Plugin 추가
    * cpp 파일이 보이는지 확인하기 위해, Actor하나 생성
